import React from 'react';

const PrioritisationPage = () => {
    return (
        <div className="p-8 bg-gray-900 text-white min-h-screen">
            <h1 className="text-3xl font-bold">Prioritisation Page (Debug View)</h1>
            <p className="mt-4">If you can see this text, the page routing is working correctly, and the problem is with one of the components that was on this page.</p>
        </div>
    );
};

export default PrioritisationPage;